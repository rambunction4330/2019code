package frc.robot


class OI internal constructor() {
    //    private static final XboxController xbox2 = new XboxController(2);

    //    public static final
    /*   internal var shootButton: Button = JoystickButton(xbox1, 1)
       internal var succButton: Button = JoystickButton(xbox1, 2)
       internal var kickButton: Button = JoystickButton(xbox1, 5)
       internal var recieveBall = RecieveBall()
       internal var shoot = Shoot()
       internal var extend = Extend()

       init {
           shootButton.whenPressed(shoot)
           succButton.whenPressed(recieveBall)
           kickButton.whenPressed(extend)
       }

       companion object {
           var xbox1 = XboxController(0)
       }*/
}
